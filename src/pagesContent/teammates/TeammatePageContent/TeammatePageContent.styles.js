const styles = {
  content: {
    height: '100%',
  },
  paddingTop: {
    paddingTop: (theme) => theme.spaces.small,
  },
  marginTop: {
    marginTop: (theme) => theme.spaces.small,
  },
  name: {
    padding: 0,
    margin: 0,
  },
};

export default styles;
