const styles = {
  content: {
    height: '100%',
  },
  marginTop: {
    marginTop: (theme) => theme.spaces.small,
  },
  paddingTop: {
    paddingTop: (theme) => theme.spaces.small,
  },
};

export default styles;
