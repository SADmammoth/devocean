const styles = {
  content: {
    height: "100%",
  },
  marginTop: {
    marginTop: (theme) => theme.spaces.small,
  },
  icon: {
    fontSize: (theme) => theme.fontSizes.bigger,
  },
};

export default styles;
