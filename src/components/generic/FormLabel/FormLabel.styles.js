const styles = {
  formLabel: {
    height: 'calc(100% + 10px)',
    width: 'calc(100% + 10px)',
    position: 'absolute',
    left: '-7px',
    top: '-7px',
  },

  invalid: {
    borderColor: (theme) => theme.text.error,
  },
};

export default styles;
