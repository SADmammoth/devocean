const styles = {
  dayBadge: {
    padding: '0',
    borderRadius: '5px',
    width: '32px',

    textAlign: 'center',
  },
  normal: {
    // background: (theme) => theme.background.dark,
    display: 'none',
  },
  active: {
    //background: (theme) => theme.background.common,
    background: (theme) => theme.background.dark,
  },
};

export default styles;
